import mongoose from 'mongoose'

const product = new mongoose.Schema({
  name: String,
  user: user_id,
  email: String,
  password: String,
  userType: String,
  createdAt: String
});



export default user;
